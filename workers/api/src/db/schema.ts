// Drizzle schema for the Matrizo D1 database.
//
// Intentionally empty for Phase 0 (scaffolding only) — the real catalog,
// user, cart, and order tables land in Phase 1 onward. Once tables are
// added here, generate a migration with `pnpm --filter @matrizo/api db:generate`
// and apply it with `pnpm --filter @matrizo/api db:migrate:local`.
export {};
